Act as a professional Frontend Developer and GIS UI/UX Designer and improve the BlueWaste map behavior. Ensure that when the map first loads, all report markers are immediately visible with their light/pulse animation, without requiring users to zoom in.

The map should automatically adjust its center and zoom (fit bounds) so all report locations appear on screen.

For now, remove or hide the barangay markers/layers and display only the waste report markers on the map to keep the interface clean and focused.

The goal is to make reported waste locations more noticeable and visually engaging on the map.