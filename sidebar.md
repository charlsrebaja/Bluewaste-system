Act as a professional UI/UX Designer and Frontend Developer and implement a collapsible sidebar in the BlueWaste dashboard.

Requirements:

Add a toggle button (hamburger/menu icon) to collapse and expand the sidebar.

When collapsed, the sidebar should shrink to icons only while hiding the menu text.

When expanded, it should show both icons and labels.

Ensure the transition animation is smooth.

The active tab indicator should still work properly even when the sidebar is collapsed.

Make sure the layout adjusts properly so the main content expands when the sidebar collapses.

Ensure the sidebar is fully responsive for desktop and mobile.

The goal is to create a clean, modern, and space-efficient dashboard layout.