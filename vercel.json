{
  "buildCommand": "npm install --include=dev && npm run backend:build && npm run web:build",
  "outputDirectory": "web/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
