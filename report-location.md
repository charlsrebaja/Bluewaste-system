Act as a professional UI/UX Designer and Frontend Developer and improve the Submit Waste Report Location feature in the BlueWaste system.

Ensure users can view an interactive map where they can click on the map to mark the waste report location. Also add a “Use Current Location (GPS)” button that automatically detects the user's location and places a marker on the map.

Requirements:

Users can click anywhere on the map to drop a marker.

Add a GPS button to automatically mark the user’s current location.

Display the selected latitude and longitude in the form fields.

Ensure the map loads properly and is responsive on mobile and desktop.

The marker should move or update if the user selects another location.

The goal is to make the location selection simple, accurate, and user-friendly when submitting a waste report. 